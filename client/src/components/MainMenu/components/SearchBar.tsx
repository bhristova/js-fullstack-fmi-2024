import styled from "styled-components"

export const SearchBar = () => {
  return (
    <SearchBarStyled />
  )
}

const SearchBarStyled = styled.input`
  border: none;
  margin: 0.5rem;
  border-radius: 4px;
`